include ../../../views/helpers/mixins.pug
//- input
mixin input(data)
  .input(class="input-"+(data.type ? data.type : '')+"")
    if data.label
      span.label=data.label
    if data.required
      input(type=data.type, name=data.name, placeholder=data.placeholder, required)
    else
      input(type=data.type, name=data.name, placeholder=data.placeholder)
//- input with icon
mixin input-icon(data)
  .input(class="input-"+(data.type ? data.type : '')+" input-icon")
    if data.label
      span.label=data.label
    .input-icon__wrap
      span.icon: +icons(data.icon,'20')
      if data.required
        input(type=data.type, name=data.name, placeholder=data.placeholder, required)
      else
        input(type=data.type, name=data.name, placeholder=data.placeholder)
//- textarea
mixin textarea(data)
  .textarea
    if data.label
      span.label=data.label
    if data.required
      textarea(placeholder=data.placeholder, name=data.name, required)
    else
      textarea(placeholder=data.placeholder, name=data.name)
//- textarea with icon
mixin textarea-icon(data)
  .textarea
    if data.label
      span.label=data.label
    .input-icon__wrap
      span.icon: +icons(data.icon,'20')
      if data.required
        textarea(placeholder=data.placeholder, name=data.name, required)
      else
        textarea(placeholder=data.placeholder, name=data.name)
//- select
mixin select(data)
  .select
    if data.label
      span.label=data.label
    if data.required
      select(name=data.name required)
        each item in data.data
          option(value="item")=item
    else
      select(name=data.name)
        each item in data.data
          option(value="item")=item
//- select with icon
mixin select-icon(data)
  .select
    if data.label
      span.label=data.label
    .select-icon__wrap
      span.icon: +icons(data.icon,'20')
      if data.required
        select(name=data.name required)
          each item in data.data
            option(value="item")=item
      else
        select(name=data.name)
          each item in data.data
            option(value="item")=item